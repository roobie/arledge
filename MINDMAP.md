# Mind Map Format - Self-Documentation

> **For AI Agents:** This mind map is your primary knowledge index. Read overview nodes [1-5] first, then follow links [N] to find what you need. Always reference node IDs. When you encounter bugs, document your attempts in relevant nodes. When you make changes, update outdated nodes immediately—especially overview nodes since they're your springboard. Add new nodes only for genuinely new concepts. Keep it compact (20-50 nodes typical). The mind map wraps every task: consult it, rely on it, update it.

[1] **Mind Map Format Overview** - A graph-based documentation format stored as plain text files where each node is a single line containing an ID, title, and inline references [2]. The format leverages LLM familiarity with citation-style references from academic papers, making it natural to generate and edit [3]. It serves as a superset structure that can represent trees, lists, or any graph topology [4], scaling from small projects (<50 nodes) to complex systems (500+ nodes) [5]. The methodology is fully detailed in PROJECT_MIND_MAPPING.md with bootstrapping tools available.

[2] **Node Syntax Structure** - Each node follows the format: `[N] **Node Title** - node text with [N] references inlined` [1]. Nodes are line-oriented, allowing line-by-line loading and editing by AI models [3]. The inline reference syntax `[N]` creates bidirectional navigation between concepts, with links embedded naturally within descriptive text rather than as separate metadata [1][4]. This structure is both machine-parsable and human-readable, supporting grep-based lookups for quick node retrieval [3].

[3] **Technical Advantages** - The format enables line-by-line overwriting of nodes without complex parsing [2], making incremental updates efficient for both humans and AI agents [1]. Grep operations allow instant node lookup by ID or keyword without loading the entire file [2]. The text-based storage ensures version control compatibility, diff-friendly editing, and zero tooling dependencies [4]. LLMs generate this format naturally because citation syntax `[N]` mirrors academic paper references they've seen extensively during training [1][5].

[4] **Graph Topology Benefits** - Unlike hierarchical trees or linear lists, the graph structure allows many-to-many relationships between concepts [1]. Any node can reference any other node, creating knowledge clusters around related topics [2][3]. The format accommodates cyclic references for concepts that mutually depend on each other, captures cross-cutting concerns that span multiple subsystems, and supports progressive refinement where nodes are added to densify understanding [5]. This flexibility makes it suitable as a universal knowledge representation format [1].

[5] **Scalability and Usage Patterns** - Small projects typically need fewer than 50 nodes to capture core architecture, data flow, and key implementations [1]. Complex topics or large codebases can scale to 500+ nodes by adding specialized deep-dive nodes for algorithms, optimizations, and subsystems [4]. The methodology includes a bootstrap prompt (linked gist) for generating initial mind maps from existing codebases automatically [1]. Scale is managed through overview nodes [1-5] that serve as navigation hubs, with detail nodes forming clusters around major concepts [3][4]. The format remains navigable at any scale due to inline linking and grep-based search [2][3].

[6] **Planner Agent (agents/planner.agent.md)** - Planning-agent configuration and operating rules for Serena: frontmatter lists tools/handoffs; mandates calling Serena MCP 'initial_instructions', loading all memories, and reading README.md; workflow is research → draft plan → iterate with strict stopping rules to never implement code [1][10].

[7] **CLI Surface & Conventions (.serena/memories/cli-surface-and-conventions.md)** - Canonical CLI patterns: `ledger <group> <action>` implemented in `ledger/cli.py`; JSON input via `--model` / `--model-file`; use Pydantic `Model.model_validate_json()`; outputs: machine JSON to stdout via `ledger/config.dump_model()` and human messages to stderr; tests in `tests/test_cli_json_output.py` and `tests/test_cli_all_json_outputs.py` [1][11].

[8] **Coverage Learnings (.serena/memories/coverage-learnings.md)** - Test coverage improvements and CI: added pytest-cov/coverage, `pyproject.toml` addopts for coverage, `.coveragerc`, CI workflow `.github/workflows/ci.yml`, and `.gitignore` updates; final measured coverage 91.02% after tests added with recommended next steps to raise `--cov-fail-under` and add web reporting [1][12].

[9] **Domain: Creditors & Payment Accounts (.serena/memories/domain-creditor-payment-accounts.md)** - Business rules and DB notes: `creditors` and `creditor_payment_accounts` schemas, `metadata` as free-form JSON, `is_default` semantics, plaintext sensitive identifiers (future encryption), monetary storage as integer cents, exporter preferences for `beancount_account`; refs: `ledger/db.py`, `ledger/models.py`, `ledger/migrations.py` [1][11].

[10] **Project General (.serena/memories/project-general.md)** - Onboarding and quickstart: use `uv` wrapper (e.g. `uv run pytest`), pytest configured in `pyproject.toml`, coverage/dev-deps noted, and README/pyproject referenced for run/test info [1].

[11] **Pydantic Models & Serialization (.serena/memories/pydantic-models-and-serialization.md)** - Model conventions: models in `ledger/models.py` (`Customer`, `Creditor`, `PaymentAccount`, `InvoiceLine`, `Invoice`); prefer `model_validate_json()`; include `model_version`; use `ledger/config.dump_model()` for canonical serialization (UTC ISO-8601 datetimes, Decimal as strings); JSON Schema via `Model.model_json_schema()` for `--json-schema` [1][7].

[12] **Test Coverage Setup (.serena/memories/test-coverage-setup.md)** - Coverage config details: pytest addopts in `pyproject.toml` (`--cov=ledger --cov-report=xml:coverage.xml --cov-report=html:htmlcov --cov-fail-under=75`), `.coveragerc` ignores, CI workflow added, local run commands (`uv run pytest`) and recommendation to raise coverage threshold once stable [8].

[13] **Beancount Notes (docs/beancount.md)** - Reference notes on double-entry bookkeeping (Beancount): introduction, accounts types (Assets, Liabilities, Equity, Income, Expenses), transactions/postings rules, trial balance, income statement, clearing/summarizing, chart-of-accounts conventions and reporting perspectives; useful background for `beancount_account` mappings used elsewhere in repo [9].

[14] **Repository Layout & Key Files** - Files and directories: `ledger/` (package: cli.py, db.py, models.py, config.py, mcp_server.py, migrations.py, __main__.py, __init__.py), `tests/` (many unit tests), `.github/workflows/ci.yml` (CI), `pyproject.toml` (metadata & deps), `README.md` (agent & CLI guidance), `.serena/memories/` (agent memories) [15][18][20].

[15] **Entry Points & CLI Invocation** - Console script `ledger` mapped to `ledger.cli:cli` in `pyproject.toml`; running `python -m ledger` uses `ledger.__main__` which calls `cli()`; `main.py` also exposes the CLI for convenience. Tests and agents use `CliRunner` or `uv run python -m ledger` [7][14].

[16] **CLI Commands & UX** - Top-level groups: `database` (initialize), `customer` (create/list), `creditor` (create/list/view) with `creditor account` subgroup (create/list), `invoice` (create/list/view/export), `mcp` (start); `schema` and `instructions` commands provide agent-facing schemas and guidance; machine outputs on stdout, human logs on stderr; exit codes non-zero on errors [7][11][14].

[17] **Pydantic Models (ledger/models.py)** - Models: `Customer`, `Creditor`, `PaymentAccount`, `InvoiceLine`, `Invoice`. Key logic: field validators coerce datetimes and decimals, InvoiceLine.model_validator computes net/vat/line_total with ROUND_HALF_UP quantize(0.01), Invoice.model_validator computes subtotal/total_vat/total and ensures created_at default UTC now; `model_version` field set from package __version__ [11].

[18] **Database Layer (ledger/db.py)** - SQLite DB `ledger.db` in CWD; table names prefixed via `config.arledge_db_prefix` (validated); init_db() creates tables (`customer`, `invoice`, `invoice_line`, `creditor`, `creditor_payment_account`, `metadata`) and ensures invoice has `creditor_id` and `currency` columns; note: when `config.IS_DEVELOPMENT` is True, init_db removes existing DB file to recreate fresh DB; create/list/get functions return Pydantic models via `model_validate`/`model_validate_json`; invoice line prices stored as culture-invariant strings, quantities as strings, metadata stored as JSON text; default-payment-account logic clears previous defaults when creating `is_default` account; invoice export writes JSON file and returns path; invoice numbering uses `INV-XXXX` format [9][17].

[19] **Config Helpers & Serialization (ledger/config.py)** - Utilities: dt_to_iso_utc / iso_to_dt (UTC aware ISO strings), decimal_to_str / decimal_to_str_currency (currency two-decimal quantize), JSON_ENCODERS mapping for Pydantic, and `dump_model()` which returns a JSON-serializable dict converting datetimes and Decimals recursively for CLI/MCP outputs [11][18].

[20] **MCP stdio Server (ledger/mcp_server.py)** - Lightweight FastMCP launcher with lazy imports; `start_mcp_stdio_server` supports `dry_run` for tests, registers tools mirroring CLI operations (customer_create/list, creditor_create/list/view, account_create/list, invoice_create/list/view/export, schema, instructions), and prints a minimal health `ping` tool; the CLI `mcp start` command lazily imports and calls this launcher [16].

[21] **Migrations Strategy (ledger/migrations.py)** - Currently a placeholder; `db.init_db()` performs idempotent table creation and simple `ensure_column` migrations; `run_migrations()` exists for future ALTER TABLE migrations [18].

[22] **Tests & Coverage (tests/)** - Test suite exercises CLI (click CliRunner, isolated_filesystem), DB CRUD integration, model property tests (Hypothesis), serialization/config tests, and CLI error branches. Pytest configured in `pyproject.toml` with default addopts including coverage; CI enforces coverage gate (80 in CI, 75 in local addopts). Key patterns: tests read/write temp DB in isolated filesystem and assert JSON outputs from stdout [7][8][13].

[23] **CI Configuration (.github/workflows/ci.yml)** - GitHub Actions job: Python 3.13 used; installs deps (editable install + pytest/cov), runs pytest with `--cov-fail-under=80` and uploads `coverage.xml` and `htmlcov/` artifacts; suggest aligning local addopts and CI thresholds [8][22].

[24] **Packaging & Dependencies (pyproject.toml)** - Project metadata: name `arledge`, version `0.1.0`, console script `ledger`; dependencies include `click`, `pydantic`, `mcp[cli]`, `beancount`, `pytest`, etc. Note: `pyproject.toml` requires Python >=3.13 while README and code support older versions — reconcile if publishing [14][23].

[25] **Known Behaviors, Tradeoffs & Risks** - Notable implementation details: sensitive identifiers (IBAN/email) stored plaintext in DB (consider encryption-at-rest) [9]; init_db may remove existing DB in development mode (risky if IS_DEVELOPMENT mis-set) [18]; decimal handling uses quantize(0.01) and ROUND_HALF_UP (financially sensible) [17]; MCP lazy imports avoid adding deps to unrelated flows [20].

[26] **Next Update Suggestions** - Actions to improve repo quality: reconcile Python-version mismatch in pyproject vs README; add migrations framework or protect init_db from accidental DB deletion in non-dev contexts; document `arledge_db_prefix` use; add encryption option for sensitive fields; raise CI coverage gate to match measured coverage; update MINDMAP when making changes [24][25].


# End of mind map nodes (update nodes as code changes happen).